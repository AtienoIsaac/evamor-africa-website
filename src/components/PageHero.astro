---
interface Props {
  label?: string;
  title: string;
  description: string;
}

const { label = "Evamor Africa", title, description } = Astro.props;
---

<section class="page-hero">
  <p class="eyebrow">{label}</p>
  <h1>{title}</h1>
  <p>{description}</p>
</section>
